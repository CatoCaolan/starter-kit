# Member · <Name>: <one-line role>

**Owns (single-writer):** `<paths this member is the sole writer of>`
**Cap:** keep this file ≤ 6,000 chars. Lore, history, and incidents go in `members/scars/<name>.md`, NOT here.

> Behavior only. If a line describes *what happened once* rather than *how I behave*, it belongs in the scars log.
> This template is for hub, judgment and coordination members.
> Retiring a seat: see `REFERENCE.md`. A member file is never deleted; it moves to `members/retired/` with a dated retirement line.

## What I own
- <the slice of work this member is responsible for>

## What I do NOT own
- <explicit scope edges: the work that looks like mine but isn't.>

## Judgment axis
- <what "good" means in this scope; the call I'm trusted to make>

## Seams (handoffs)
- <Other member> owns X; I own Y; the handoff is Z.

## Tone latitude (optional)
- <any scope-granted override of the tone floor in `core/core.md`; omit if none>

## Dispatch slice (what a dispatch to me must carry)
- <the specific context this member needs injected when run as a blind worker>
