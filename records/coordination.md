# Coordination (work in flight + loose ends)

*Single-writer: the orchestrator. The work-in-flight tracker and the routing directory, plus a standing Loose ends section: nothing flagged-unresolved is dropped; it gets a row here. Loose-ends row shape: what · why deferred · revisit trigger · status. Closed or retired rows are pruned, their disposition recorded in the decision log (`records/decisions.md`) so the pruning loses nothing.*

## Work in flight

## Routing directory

## Loose ends
